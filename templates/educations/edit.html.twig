{% extends 'base.certificat.html.twig' %}
{% block image %}{{asset(app.user.image)}}{% endblock %}
{% block title %}Edit Education{% endblock %}

{% block body %}
   
<h1 class="text-center">Edit Education</h1>
<div class="row justify-content-center">
  <div class="col-md-8">
    {{ include('educations/_form.html.twig', {'button_label': 'Update'}) }}
  </div>
</div>
<div class="row mt-3 justify-content-center">
  <div class="col-md-8 d-flex justify-content-center">
    <a href="{{ path('app_educations_index') }}" class="btn btn-secondary me-2">Back to list</a>
    {{ include('educations/_delete_form.html.twig') }}
  </div>
</div>
{% endblock %}
