{% extends 'base.certificat.html.twig' %}
{% block image %}{{asset(app.user.image)}}{% endblock %}
{% block title %}Edit Parametres{% endblock %}

{% block body %}

<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header">
          <h1 class="text-center">Edit Parametres</h1>
        </div>
        <div class="card-body">
          {{ include('parametress/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="card-footer d-flex justify-content-between align-items-center">
          <a href="{{ path('app_parametress_index') }}" class="btn btn-sm btn-primary"><i class="fas fa-arrow-circle-left mr-2"></i>Back to List</a>
          <div>
            <a href="{{ path('app_parametress_edit', {'id': parametre.id}) }}" class="btn btn-sm btn-primary mr-2"><i class="fas fa-edit"></i> Edit</a>
            {{ include('parametress/_delete_form.html.twig') }}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
