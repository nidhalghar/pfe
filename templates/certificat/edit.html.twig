{% extends 'base.certificat.html.twig' %}
{% block image %}{{asset(app.user.image)}}{% endblock %}
{% block title %}Edit Certificat{% endblock %}
{% block stylesheets %} 
<style>
  .fa-arrow-left {
    margin-right: 5px;
  }
</style>{% endblock %}
{% block body %}
    <body>
  <div class="container">
    <div class="form-container">
      <h1 class="text-center">Edit Certificat</h1>
      {{ include('certificat/_form.html.twig', {'button_label': 'Update'}) }}
      <div class="text-center mt-3">
        <a href="{{ path('app_certificat_index') }}" class="btn btn-secondary mr-2"><i class="fas fa-arrow-left"></i> Back to list</a>
       <br>
        {{ include('certificat/_delete_form.html.twig') }}
      </div>
    </div>
  </div>
</body>

{% endblock %}
