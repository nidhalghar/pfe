{% extends 'base.certificat.html.twig' %}
{% block image %}{{asset(app.user.image)}}{% endblock %}
{% block title %}Edit Competences{% endblock %}

{% block body %}
 <div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header">
          <h1 class="text-center">Edit Competences</h1>
        </div>
        <div class="card-body">
          {{ include('competences/_form.html.twig', {'button_label': 'Update'}) }}
          <div class="text-center mt-3">
            <a href="{{ path('app_competences_index') }}" class="btn btn-secondary mr-2"><i class="fas fa-arrow-left"></i> Back to list</a>
            {{ include('competences/_delete_form.html.twig') }}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{% endblock %}
