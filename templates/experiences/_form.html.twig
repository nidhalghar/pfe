<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <h2 class="text-center mb-4">Expérience</h2>
      <div class="card">
        <div class="card-body">
          {{ form_start(form) }}
          <div class="form-group">
            {{ form_row(form.Nomexperience, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Experience', 'id': 'nomExperience'}}) }}
          </div>
          <div class="form-group">
            {{ form_row(form.datededebut, {'attr': {'id': 'dateDebut'}}) }}
          </div>
          <div class="form-group">
            {{ form_row(form.datedefin, {'attr': {'id': 'dateFin'}}) }}
          </div>
          <div class="form-group">
            {{ form_row(form.Description, {'attr': {'class': 'form-control form-control-lg', 'placeholder': 'Ecrire ici..*', 'id': 'description'}}) }}
          </div>
          <div class="text-center">
            <button class="btn btn-lg btn-primary">{{ button_label|default('Save') }}</button>
            <a href="{{ path('app_experiences_index') }}" class="btn btn-lg btn-secondary mx-2">Back to list</a>
            {{ form_end(form) }}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
